package ProjectPackage;

public class TaskPriority implements Comparable<Task>{
    //custom comparator
    //Isla
    //can compare by completion

    // Ask Sophia about how to compared Tasks since they're of String type
    @Override
    public int compareTo(Task o) {
        return 0;
    }
}
